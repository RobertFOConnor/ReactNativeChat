import {StyleSheet} from "react-native";
import {colors} from '../../common/colors';

export const styles = StyleSheet.create({
    textInput: {
        marginVertical: 10,
        backgroundColor: colors.text_field,
        paddingHorizontal: 15,
        borderRadius: 20,
        width: 250,
        height: 40,
        elevation: 2,
    },
});
